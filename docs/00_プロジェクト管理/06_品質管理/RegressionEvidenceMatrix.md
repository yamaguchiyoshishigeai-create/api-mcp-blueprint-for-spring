# Regression Evidence Matrix

## TSK-044

| 項目 | 内容 |
|---|---|
| TSK ID | TSK-044 |
| 完了条件 | AI実装指示書に旧注意文言を再出力しない |
| 対応テストクラス | ImplementationInstructionGeneratorTest / BlueprintGenerationRegressionTest / GeneratedOutputForbiddenPhraseGuardTest |
| 対応テストメソッド | `generatesTargetApplicationInstructionsWithoutApimInternalImplementationScope` / `assertDocumentsKeepRequiredSectionsAndOutOfScope` / `generatedBlueprintAndImplementationInstructionsDoNotContainAnyForbiddenPhrase` |
| 正例条件 | 対象業務アプリケーション向けの実装指示と必須セクションが出力される |
| 否定条件 | `APIM for Spring本体の改修指示ではない` を含む禁止語が blueprint markdown / implementation instructions に含まれない |
| 手動確認 | Render公開前確認ではプレビュー画面でも禁止語再発がないことを確認する |

## TSK-064

| 項目 | 内容 |
|---|---|
| TSK ID | TSK-064 |
| 完了条件 | Markdown設計書にAPIM本体MVP制約が混入しない |
| 対応テストクラス | MarkdownDocumentGeneratorTest / GeneratedOutputForbiddenPhraseGuardTest |
| 対応テストメソッド | `usesInputDrivenOverviewInsteadOfApimInternalMvpText` / `outOfScopeSectionIsExpressedAsLaterPhaseDecisions` / `generatedBlueprintAndImplementationInstructionsDoNotContainAnyForbiddenPhrase` |
| 正例条件 | 設計対象概要が入力要件ベースで出力され、`## 14. 後続フェーズで具体化する事項` として後続判断事項が出力される |
| 否定条件 | `APIM for Spring の初期MVP向け設計成果物。` と `初期MVPで実装しないこと` が blueprint markdown / implementation instructions に含まれない |
| 手動確認 | Render公開前確認では blueprint preview の `## 1` と `## 14` が対象システム基準の文脈で出力されることを確認する |

## TSK-065

| 項目 | 内容 |
|---|---|
| TSK ID | TSK-065 |
| 完了条件 | AI実装指示書に固定的な「実装しないこと」を出力しない |
| 対応テストクラス | ImplementationInstructionGeneratorTest / BlueprintGenerationRegressionTest / GeneratedOutputForbiddenPhraseGuardTest |
| 対応テストメソッド | `generatesTargetApplicationInstructionsWithoutApimInternalImplementationScope` / `assertDocumentsKeepRequiredSectionsAndOutOfScope` / `generatedBlueprintAndImplementationInstructionsDoNotContainAnyForbiddenPhrase` |
| 正例条件 | AI実装指示書で `## 10. 後続フェーズで具体化する事項` が出力され、後続AIにも実装禁止ではなく追加設計・実装判断事項として扱う旨が出力される |
| 否定条件 | `## 10. 実装しないこと` と旧固定禁止文言が blueprint markdown / implementation instructions に含まれない |
| 手動確認 | Render公開前確認では implementation instructions preview の `## 10` が後続判断事項として出力されることを確認する |

## TSK-001〜TSK-056 段階棚卸しサマリ

詳細棚卸しは `RegressionEvidenceInventory_TSK001_TSK056.md` を正本とする。

| TSK ID | 件名 | 証跡分類 | 対応証跡 | 正例条件 | 否定条件 | 不足時の後続対応 |
|---|---|---|---|---|---|---|
| TSK-001 | 初期READMEとdocs入口文書の整合を確認する | 手動確認証跡で十分 | 文書レビュー、リンク確認、課題一覧の状態同期 | 必要な文書が存在し、入口文書から到達できること | 文書欠落、入口リンク不整合、旧記述残存が再発しないこと | 大幅な仕様文書変更時は静的リンク検査またはレビュー記録を追加 |
| TSK-002 | APIM-001立ち上げ方針書を企画正本として配置する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-003 | APIM-003 MVP最小要件定義書を作成する | 手動確認証跡で十分 | 文書レビュー、リンク確認、課題一覧の状態同期 | 必要な文書が存在し、入口文書から到達できること | 文書欠落、入口リンク不整合、旧記述残存が再発しないこと | 大幅な仕様文書変更時は静的リンク検査またはレビュー記録を追加 |
| TSK-004 | APIM-004 基本設計書を作成する | 手動確認証跡で十分 | 文書レビュー、リンク確認、課題一覧の状態同期 | 必要な文書が存在し、入口文書から到達できること | 文書欠落、入口リンク不整合、旧記述残存が再発しないこと | 大幅な仕様文書変更時は静的リンク検査またはレビュー記録を追加 |
| TSK-005 | APIM-005 Web MVPを実装する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-006 | Web MVP初回レビューと改善を実施する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-007 | APIM for Spring に Maven Wrapper を追加する | 手動確認証跡または静的検査 | 公開前チェックリスト、ローカル/CIログ、設定ファイル静的確認 | 対象環境で起動・build・公開前確認が成立すること | 必要パッケージ不足、起動不能、公開前確認漏れが再発しないこと | 公開・環境変更時は確認記録を追加 |
| TSK-008 | Windows環境で spring-boot:run 起動失敗を調査する | 手動確認証跡または静的検査 | 公開前チェックリスト、ローカル/CIログ、設定ファイル静的確認 | 対象環境で起動・build・公開前確認が成立すること | 必要パッケージ不足、起動不能、公開前確認漏れが再発しないこと | 公開・環境変更時は確認記録を追加 |
| TSK-009 | 初期作業ブランチの扱いを確認する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-010 | Domain Vocabulary / Resource Naming を改善する | 手動確認証跡で十分 | 個票、PR、CI、手動確認記録 | 個票の完了条件に対応する成果物が確認できること | 解決済み状態と成果物の不整合が再発しないこと | 不足が見つかれば後続TSKで個別対応 |
| TSK-011 | 生成品質回帰テスト自動化を整備する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-012 | タスク採番方針をTSK正規IDへ是正する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-013 | 改善タスク管理の状態フォルダ保持ルールを追加する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-014 | ChatExec結果ファイルのgitignore標準化を追加する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-015 | APIM入力方式を構造化入力型へ再設計する | 手動確認証跡で十分 | 個票、PR、CI、手動確認記録 | 個票の完了条件に対応する成果物が確認できること | 解決済み状態と成果物の不整合が再発しないこと | 不足が見つかれば後続TSKで個別対応 |
| TSK-016 | 外部実行補助ログのgitignore標準化を追加する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-017 | Windows Git内部メンテナンス対話プロンプトを抑制する | 手動確認証跡または静的検査 | 公開前チェックリスト、ローカル/CIログ、設定ファイル静的確認 | 対象環境で起動・build・公開前確認が成立すること | 必要パッケージ不足、起動不能、公開前確認漏れが再発しないこと | 公開・環境変更時は確認記録を追加 |
| TSK-018 | 生成成果物Markdownをダウンロード可能にする | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-019 | チェックボックス群に全選択・全解除を追加する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-020 | システム種別プリセットから標準ドメイン・標準操作を自動投入する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-021 | ドメイン候補カタログをカテゴリ別に拡充する | 手動確認証跡で十分 | 文書レビュー、リンク確認、課題一覧の状態同期 | 必要な文書が存在し、入口文書から到達できること | 文書欠落、入口リンク不整合、旧記述残存が再発しないこと | 大幅な仕様文書変更時は静的リンク検査またはレビュー記録を追加 |
| TSK-022 | 複数ドメイン・複合システム入力モデルを設計する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-023 | 待ちフェーズの短時間ポーリングとフォローアップ入力許容ルールを明文化する | 手動確認証跡で十分 | 個票、PR、CI、手動確認記録 | 個票の完了条件に対応する成果物が確認できること | 解決済み状態と成果物の不整合が再発しないこと | 不足が見つかれば後続TSKで個別対応 |
| TSK-024 | squash merge後の作業ブランチ削除・prune運用を標準化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-025 | 対象システム種別プリセットを複数選択UIへ拡張する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-026 | 対象ドメインプリセットを整理しカテゴリ別ドメイン候補へ統合する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-027 | カテゴリ別ドメイン候補をnode表示へ整理する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-028 | 順次タスクmergeゲートを標準化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-029 | 複数ドメイン設計書を現行UI改善完了状態へ同期する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-030 | 複数システム・複数ドメイン入力を内部モデル化する | 手動確認証跡で十分 | 文書レビュー、リンク確認、課題一覧の状態同期 | 必要な文書が存在し、入口文書から到達できること | 文書欠落、入口リンク不整合、旧記述残存が再発しないこと | 大幅な仕様文書変更時は静的リンク検査またはレビュー記録を追加 |
| TSK-031 | NormalizedBlueprintInput を導入する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-032 | Markdown設計書とAI実装指示書にドメイン境界を表示する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-033 | API Generatorを複数ドメイン対応へ拡張する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-034 | MCP Generatorを複数ドメイン対応へ拡張する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-035 | ChatExec2方式を並行作業対応の外部実行方式として仕様化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-036 | ChatExec2方式にWindowsエクスプローラー実行UXを追加する | 手動確認証跡または静的検査 | 公開前チェックリスト、ローカル/CIログ、設定ファイル静的確認 | 対象環境で起動・build・公開前確認が成立すること | 必要パッケージ不足、起動不能、公開前確認漏れが再発しないこと | 公開・環境変更時は確認記録を追加 |
| TSK-037 | ChatExec2方式に再配布ファイル名一意化と自己参照方針を追加する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-038 | ChatExec2ローカル詳細結果ディレクトリをgitignore標準化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-039 | AI作業分担方針を横断運用規程として仕様化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-040 | AI実装指示書のドメイン境界文言を同期する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-041 | Render公開準備を整備する | 手動確認証跡または静的検査 | 公開前チェックリスト、ローカル/CIログ、設定ファイル静的確認 | 対象環境で起動・build・公開前確認が成立すること | 必要パッケージ不足、起動不能、公開前確認漏れが再発しないこと | 公開・環境変更時は確認記録を追加 |
| TSK-042 | 初回訪問者向け体験ルートを実装する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-043 | 初回訪問者向け価値理解・行動導線を再設計する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-044 | AI実装指示書を生成対象アプリ向けに修正する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-045 | プレビュー画面にダウンロード導線と入力状態復元導線を追加する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-046 | 初回訪問者向けサンプル業務パターンを複数化する | 自動回帰テスト必須 | 既存のSpring/Mavenテスト、生成物回帰テスト、または後続で追加する対象テスト | 対象機能が期待どおり出力・表示・遷移すること | 旧仕様、欠落、固定文言、導線消失、複数ドメイン非対応等が再発しないこと | 不足時は対象機能ごとの後続TSKで自動テストを追加 |
| TSK-047 | ルート直下ノイズ整理とignore方針を整備する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-048 | CIポーリング終了条件固定方針を明文化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-049 | 直近完了タスクの課題管理状態を整理する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-050 | Merge後cleanup・local sync自動統合方針を明文化する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-051 | TSK-050完了状態を課題管理へ反映する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-052 | ルート直下CodexExec指示書の配置を整理する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-053 | 運用ルール改定の横展開要否を判断する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-054 | Spring-Tool-Development-TemplateへTSK-047/048/050系運用ルールを部分横展開する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-055 | Q-Scout-for-SpringへTSK-047/048/050系運用ルールを部分横展開する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |
| TSK-056 | portfolioへTSK-047/048/050系運用ルールを軽量横展開する | 静的検査または手動確認証跡 | 課題一覧、対象文書、git状態、CI結果、ChatExec2 summary.result | 文書・管理状態・ignore・cleanup・syncの期待状態が残ること | 旧運用重複、未同期、不要ファイル混入、cleanup漏れが再発しないこと | 作業完了報告に確認結果を記録 |

## 今後のTSK解決時の記録要件

新規TSKまたは既存TSKを解決済みにする場合は、`回帰証跡記録ルール.md` に従い、回帰証跡の要否、自動テスト、メタテスト、静的検査、手動確認証跡、対象外根拠、後続TSKのいずれかを記録する。
