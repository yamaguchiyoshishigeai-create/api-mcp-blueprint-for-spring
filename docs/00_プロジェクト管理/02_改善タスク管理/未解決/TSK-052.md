# TSK-052 ルート直下CodexExec指示書の配置を整理する

## 状態

未解決

## 優先度

High

## 主担当候補

ChatGPT(リポジトリ編集)

## 背景

TSK-047で、ChatExec / ChatExec2 / Codex 連携で発生するローカル一時ファイルとルート直下整理方針を明文化した。

同方針では、`CodexExec_*.md` は一律ignoreしないが、正規文書として保存する場合は `docs/00_プロジェクト管理/03_実装支援AI指示/` 配下へ配置し、単発投入用・再生成可能・保存不要なものは作業完了後にローカル削除することとしている。

一方、main上には `CodexExec_TSK046_multi_sample_entry_route.md` がリポジトリルート直下に残っている。

これはTSK-046のCodex投入用指示書であり、保存価値があるならdocs配下へ移動し、既存docsと重複するなら削除する必要がある。

## 対応内容

1. ルート直下 `CodexExec_TSK046_multi_sample_entry_route.md` の内容を確認する。
2. `docs/00_プロジェクト管理/03_実装支援AI指示/` 配下に同等または上位のTSK-046指示書が存在するか確認する。
3. 重複している場合は、ルート直下ファイルを削除する。
4. 保存すべき内容がルート直下ファイルにしかない場合は、docs配下へ移動する。
5. 課題一覧と個票の状態を対応結果に合わせて更新する。

## 実施しないこと

- TSK-046実装の再修正。
- Codex投入用指示書の内容改変による仕様変更。
- `CodexExec_*.md` の一律ignore化。
- 他リポジトリへの横展開。
- Render公開準備。

## 完了条件

- ルート直下に `CodexExec_TSK046_multi_sample_entry_route.md` が残っていないこと。
- 保存すべき場合はdocs配下へ移動済みであること。
- 重複で削除する場合は削除理由が個票に明記されていること。
- `.gitignore` 方針と矛盾していないこと。
- PRのCIがsuccessであること。
- mainへmerge済みであること。
