# TSK-054 Spring-Tool-Development-TemplateへTSK-047/048/050系運用ルールを部分横展開する

## 状態

解決済み

## 優先度

Medium

## 主担当候補

ChatGPT(リポジトリ編集)

## 関連PR

- Spring-Tool-Development-Template PR #32: TSK-054: APIM運用ルール改定をSpringテンプレートへ部分横展開する

## 背景

TSK-053で、APIM for Springに追加されたTSK-047 / TSK-048 / TSK-050系運用ルールについて、関連リポジトリへの横展開要否を判断した。

`Spring-Tool-Development-Template` はPR #31で運用ルールHubとCodex投入ゲートを横展開済みである。一方、TSK-047 / TSK-048 / TSK-050で追加した以下の方針は完全一致では確認できなかった。

1. ローカル一時ファイルとルート直下整理方針。
2. CIポーリング終了条件固定方針。
3. Merge後cleanup・local sync自動統合方針。

Spring系派生ツール開発テンプレートでは、ChatExec2、Codex、CI、branch cleanupを利用する可能性が高いため、APIM固有の例を抽象化したうえで部分横展開する。

## 対応結果

1. Spring-Tool-Development-Template側でPR #32を作成し、TSK-047 / TSK-048 / TSK-050系運用ルールをテンプレート向けに部分横展開した。
2. 主に以下の運用規程を追加または更新した。
   - ローカル一時ファイルとルート直下整理方針。
   - CIポーリング終了条件固定方針。
   - Merge後cleanup・local sync自動統合方針。
   - 運用ルール索引から上記3規程への参照導線。
3. CI `template-checks #177` のsuccessを確認した。
4. PR #32はmainへsquash merge済みであることを確認した。
5. merge後のremote branch cleanup、local prune、local main syncが完了済みであることを確認した。
6. Q-Scout-for-Spring / portfolioへの横展開はTSK-055 / TSK-056で別途扱う方針として整理した。

## 実施しないこと

- APIM固有文書を無条件コピーすること。
- 実装コード変更。
- CI workflow変更。
- Q-Scoutまたはportfolioへの同時反映。

## 完了条件

- `Spring-Tool-Development-Template` に必要な運用ルールが部分反映されていること。
- テンプレートとして再利用可能な抽象表現になっていること。
- PRのCIがsuccessであること。
- mainへmerge済みであること。
- merge後cleanupとlocal syncが完了していること。
