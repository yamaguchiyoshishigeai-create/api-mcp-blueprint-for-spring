# TSK-052 ルート直下CodexExec指示書の配置を整理する

## 状態

解決済み

## 優先度

High

## 主担当候補

ChatGPT(リポジトリ編集)

## 背景

TSK-047で、ChatExec / ChatExec2 / Codex 連携で発生するローカル一時ファイルとルート直下整理方針を明文化した。

同方針では、`CodexExec_*.md` は一律ignoreしないが、正規文書として保存する場合は `docs/00_プロジェクト管理/03_実装支援AI指示/` 配下へ配置し、単発投入用・再生成可能・保存不要なものは作業完了後にローカル削除することとしている。

一方、main上には `CodexExec_TSK046_multi_sample_entry_route.md` がリポジトリルート直下に残っていた。

確認の結果、当該ファイルはTSK-046のCodex投入用指示書であり、正規保存価値があるため、削除ではなくdocs配下へ移動する方針とした。

## 対応結果

1. ルート直下 `CodexExec_TSK046_multi_sample_entry_route.md` の内容を確認した。
2. 既存docs配下に同等の保存済みファイルがないことを確認した。
3. 保存価値があるため、`docs/00_プロジェクト管理/03_実装支援AI指示/CodexExec_TSK046_multi_sample_entry_route.md` へ移動した。
4. ルート直下の旧ファイルを削除した。
5. TSK-052個票と課題一覧を解決済みに同期した。

## 実施しなかったこと

- TSK-046実装の再修正。
- Codex投入用指示書の内容改変による仕様変更。
- `CodexExec_*.md` の一律ignore化。
- 他リポジトリへの横展開。
- Render公開準備。

## 完了条件確認

- ルート直下に `CodexExec_TSK046_multi_sample_entry_route.md` が残っていない。
- 保存すべき内容はdocs配下へ移動済み。
- `.gitignore` 方針と矛盾していない。
- PRのCI successとmain mergeをもって完了。
