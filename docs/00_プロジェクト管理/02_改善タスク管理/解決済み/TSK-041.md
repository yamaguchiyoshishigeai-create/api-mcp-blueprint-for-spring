# TSK-041 Render公開準備を整備する

- ID: TSK-041
- 件名: Render公開準備を整備する
- 状態: 解決済み
- 優先度: High
- 主担当候補: ChatGPT(リポジトリ編集) / Codex
- 関連タスク:
  - TSK-042 初回訪問者向け体験ルートを実装する

## 背景

APIM for Spring はローカルMVPとして、API設計書、MCP設計候補、API/MCP対応表、AI実装指示書を生成できる状態になっている。

一方で、外部訪問者が実際に触れる公開環境はまだ整備されていない。Renderへ登録し、公開URLからWeb MVPへアクセスできる状態にする必要がある。

ただし、公開前に初回訪問者が価値を理解しやすい体験ルートを整える方が効果的であるため、TSK-042を先行実施し、その後TSK-041でRender公開準備を行う。

## 目的

APIM for Spring をRenderへ登録・公開できるよう、起動設定、環境変数、README、疎通確認手順を整備する。

## 実施すること

- Render向けの起動方式を整理する。
- Java 17 / Maven Wrapper を前提としたbuild/start手順を整理する。
- Renderの `PORT` 環境変数にSpring Bootが追従できるか確認し、必要に応じて設定を追加する。
- READMEへRenderデプロイ手順を追加する。
- 公開後の疎通確認観点を整理する。
- 必要に応じて簡易health endpointまたは既存 `/` アクセス確認手順を明確化する。

## 実施しないこと

- DB永続化。
- 外部LLM API連携。
- 認証・認可の本格実装。
- 完全動作するMCPサーバー実装。
- 本番運用レベルの監視・アラート設計。

## 完了条件

- RenderでAPIM for Springを公開するためのbuild/start設定が明確になっている。
- READMEにRender公開手順が記載されている。
- 公開URLで `/` にアクセスし、初回体験ルートを試せる前提が整っている。
- TSK-042で実装する初回体験導線との整合が取れている。

## 完了実績

- 対象PR: APIM PR #102
- 対象ブランチ: `docs/tsk-041-render-publish-prep`
- CI: `template-checks #119` success
- main反映: PR #102 を main へ squash merge 済み
- merge commit: `19c912c80884e422cd8afa09954d3dec427dfd11`
- 反映内容:
  - `Dockerfile` を追加
  - `render.yaml` を追加
  - `src/main/resources/application.properties` に `server.port=${PORT:8080}` を追加
  - `docs/50_実装/Render公開準備.md` を追加
  - `README.md` にRender公開準備の参照導線と概要手順を追加
- remote branch cleanup: `docs/tsk-041-render-publish-prep` 削除済み
- GitHub側branch残存確認: `docs/tsk-041-render-publish-prep` 検索結果0件
- local prune: 完了済み
- local main sync: `main` が `origin/main` に追従済み
- local main HEAD: `19c912c80884e422cd8afa09954d3dec427dfd11` と一致
- current branch: `main`
- worktree: clean
- local work branch / remote tracking branch: 削除済み
- ChatExec2結果: `[OK] ChatExec2_Cleanup_Sync_APIM_PR102_TSK041_R01_DONE`

## 対象外として残す事項

- Render上での実サービス作成。
- Render公開URLの確定。
- 公開判断。
- 本番運用レベルの監視・アラート設計。

## 後続

- Render Dashboard上での実サービス作成と公開URL確定はユーザー(人)の外部サービス操作として扱う。
- 現時点の正規未解決TSKは、この同期PR完了後に0件となる見込み。
