# TSK-040 AI実装指示書のドメイン境界文言を同期する

- ID: TSK-040
- 件名: AI実装指示書のドメイン境界文言を同期する
- 状態: 解決済み
- 優先度: High
- 主担当候補: ChatGPT(リポジトリ編集)
- 実装PR: #69
- merge後コミット: 1f254121af1712715a0b9802e8df8ab3e6400f13

## 背景

TSK-033でAPI Generatorの複数ドメイン対応、TSK-034でMCP Generatorの複数ドメイン対応が完了した後も、AI実装指示書にTSK-032時点の「Controller、DTO、tool、resource、promptのドメイン別分割は行わない」という古い制約文言が残っていた。

この文言は現行実装と矛盾するため、TSK-033/TSK-034完了後の状態に合わせて同期した。

## 実施したこと

- `ImplementationInstructionGenerator` の古いTSK-032限定文言を削除。
- Controller、DTO、tool、resource、prompt候補はドメイン境界を意識して生成される旨へ更新。
- 完全な実装分割、実行可能MCPサーバー生成、MCP仕様ファイルの完全生成までは行わない旨を追記。
- `ImplementationInstructionGeneratorTest` を更新し、旧文言が出力されないことを確認。

## 実施しなかったこと

- API Generator / MCP Generator の追加実装。
- 完全動作するMCPサーバー実装。
- MCP仕様ファイルの完全生成。

## 完了確認

- PR #69 がmainへsquash merge済み。
- GitHub Actions `template-checks` #77 がsuccess。
- `checks` / `windows-wrapper-check` がsuccess。
- 作業ブランチ削除・prune確認済み。
